{
    "name": "backend",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "esbuild src/index.ts src/preSignup.ts src/notify.ts --bundle --platform=node --target=node20 --outdir=dist --format=cjs",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@aws-lambda-powertools/logger": "^2.8.0",
        "@aws-lambda-powertools/metrics": "^2.8.0",
        "@aws-sdk/client-cognito-identity-provider": "^3.650.0",
        "@aws-sdk/client-dynamodb": "^3.650.0",
        "@aws-sdk/client-ses": "^3.650.0",
        "@aws-sdk/lib-dynamodb": "^3.650.0",
        "hono": "^4.6.0",
        "zod": "^3.23.8"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.145",
        "@types/node": "^25.6.0",
        "@vitest/coverage-v8": "^2.1.1",
        "esbuild": "^0.23.1",
        "typescript": "^5.6.2",
        "vitest": "^2.1"
    }
}
